import 'package:flutter/material.dart';

class RobotsPage extends StatelessWidget {
  const RobotsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Robots'),);
  }
}
